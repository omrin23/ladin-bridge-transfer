# Colab already provides: torch, numpy, scipy, scikit-learn, pandas,
# matplotlib, tqdm, huggingface_hub — do NOT list them here or pip will
# try to downgrade them and cause numpy ABI crashes.

transformers>=4.44.2
peft>=0.12.0
datasets>=2.20.0
accelerate>=0.34.0
sentencepiece>=0.2.0
sacrebleu==2.4.3
pyyaml>=6.0.2
torchao>=0.16.0
